# simple_sc_examples

## Simple SystemC examples to make sure your SystemC is installed correctly.
### Makefile-based Example
```{bash}
cd makefile-based
make all
./simple
```

### CMake-based Example
```{bash}
cd cmake-based
mkdir build
cd build
cmake
./first_systemc
```
